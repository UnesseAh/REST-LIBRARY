spring:
  application:
    name: rest-library
  datasource:
    url: jdbc:postgresql://localhost:5432/library
    username: postgres
    password: admin
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        highlight_sql: true
    open-in-view: true